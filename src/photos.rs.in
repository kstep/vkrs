
#[derive(Debug, PartialEq, Eq, Deserialize)]
pub struct Photo {
    pub id: Id,
    pub album_id: Id,
    pub owner_id: OwnerId,
    pub user_id: Id,
    pub photo_75: String,
    pub photo_130: String,
    pub photo_604: String,
    pub width: u16,
    pub height: u16,
    pub text: String,
    pub date: Timestamp,
}

