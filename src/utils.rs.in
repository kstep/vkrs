#[derive(Debug, PartialEq, Eq, Deserialize, Clone)]
pub struct LinkStatus {
    pub status: String,
    pub link: String, // URL
}

#[derive(Debug, PartialEq, Eq, Deserialize, Clone)]
pub struct ScreenNameInfo {
    #[serde(rename="type")]
    pub kind: String,
    pub object_id: Id,
}
